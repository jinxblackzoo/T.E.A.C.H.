# Package initialization file
# This file is intentionally left empty to mark the directory as a Python package.
# Importiere mögliche Module, damit sie von außen sichtbar sind
# (Registrierung erfolgt aber zur Laufzeit durch Aufruf `modules.<modul>.register(app)`)
from .core.app import TEACH
